"""D8: read-only after-MS smoke arena over the frozen `afterms_nightly_v1` campaign.

Everything here consumes D7 (`artifacts/afterms_nightly_v1`,
`data/shards/afterms_nightly_v1`) as read-only, immutable input. See
`legacy_d7_adapter` for the frozen-contract boundary.
"""
